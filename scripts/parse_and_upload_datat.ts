class UploadClass {
    password: string;
    url: string;
    primaryName: string;
    plusOne: boolean;

    secondaryName?: string;
}


const parseAndUploadData = () => {


    const cells = [];

    let uploads = [];


    cells.map(async (val, ind) => {

        // Get the url
        const url = ''

        // Get the password
        const password = ''

        // check to see if plus one
        // check cell below

        uploads.push(UploadClass())
    })

}